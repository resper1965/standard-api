{
  "name": "@standard/mcp-server",
  "version": "1.0.0",
  "description": "Standard GRC Platform MCP Server Integration",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "bin": {
    "standard-mcp": "bin/standard-mcp.js"
  },
  "scripts": {
    "build": "tsup src/index.ts --format esm,cjs --dts",
    "dev": "tsup src/index.ts --format esm,cjs --watch",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.1",
    "commander": "^12.0.0",
    "hono": "4.12.31",
    "zod": "^4.4.0",
    "zod-to-json-schema": "^3.23.5"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "tsup": "^8.3.5",
    "typescript": "6.0.3"
  }
}
