# Implementation Plan: B2B M2M Integration & SaaS Governance

**Track ID:** b2b-m2m-integration_20260507
**Spec:** spec.md
**Created:** 2026-05-07
**Status:** [x] Completed

## Overview

Deploy the infrastructure for API Keys integration by linking our middleware `auth.middleware.ts` with newly deployed integration endpoints (`integration.routes.ts`) allowing 3rd party consumption of Agentic Models. Ensure the integration endpoints strictly validate M2M auth rules while formalizing SaaS Management definitions within the documentation for Superadmins and Tenant Admins.

## Phase 1: Authentication & Schema Foundation

Establish the structural contracts that validate incoming Machine integrations.

### Tasks

- [x] Task 1.1: Ensure Database schemas exist for `api_keys`, `organizations`, and `tenants`.
- [x] Task 1.2: Add Auth extraction Logic inside `apps/api-gateway/src/middleware/auth.middleware.ts` for handling `Bearer standard_live_`.
- [x] Task 1.3: Expose `AnalyzeRawTextRequestSchema` inside `packages/schemas`.

### Verification

- [x] TypeScript compiler accepts schema modifications without circular dependencies.

## Phase 2: Core External Text-Analysis Endpoint

Assemble the endpoint acting as the main gateway for external payloads into the LLM logic.

### Tasks

- [x] Task 2.1: Write `integration.routes.ts`.
- [x] Task 2.2: Fetch Agent from `AgentRuntimeService` and enforce the schema context maps.
- [x] Task 2.3: Bridge observability traces (via Audit & Metrics Service).
- [x] Task 2.4: Inject `integrationRoutes` onto the primary Gateway execution list `app.ts`.

### Verification

- [x] `POST /api/v1/integrations/assessments/:assessmentId/analyze-text` compiles properly under HTTP routing. Wait for Queue async mechanisms to prevent timing out.

## Phase 3: SaaS Core Routing & RBAC Restrictions

Solidify endpoints necessary to create subscriptions and lock them from unintended integrations.

### Tasks

- [x] Task 3.1: Ensure endpoints mapped in `api-keys.routes.ts`, `tenants.routes.ts`, and `organizations.routes.ts` block `context.actorId === "m2m-agent"` logically.
- [x] Task 3.2: Verify API Key deletion, creation, and fetching is working for authentic Tenant Admins.

### Verification

- [x] Privilege escalation is prevented by strict if-blocks rejecting keys masking as global managers.

## Phase 4: Integrator Experience & B2B Prompt Package

Furnish external stakeholders and Tenant Admins with precise tools avoiding operational back-and-forth overhead.

### Tasks

- [x] Task 4.1: Draft `B2B_INTEGRATION_GUIDE.md` inside `docs/api`.
- [x] Task 4.2: Embed explicit boundaries delineating Superadmin roles vs Tenant Admin responsibilities.
- [x] Task 4.3: Provide an exact, Universal AI Coding prompt targeting `Cursor/Claude/Copilot` to simplify client integrations.

### Verification

- [x] The generated Markdown matches GFM (GitHub Flavored Markdown) standard formatting.

## Final Verification

- [x] All acceptance criteria met
- [x] Documentation perfectly covers both endpoints and organizational management rules.
- [x] Ready for review by Principal Architect (`resper@bekaa.eu`).

---

_Generated by Conductor. Tasks will be marked [~] in progress and [x] complete._
