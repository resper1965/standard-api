{
  "name": "@standard/docs",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "typecheck": "node -e \"const v=parseInt(process.versions.node.split('.')[0]);if(v<22){console.log('astro check skipped on Node '+process.versions.node);process.exit(0);}else{process.exit(1);}\" || astro check"
  },
  "dependencies": {
    "astro": "^6.3.3",
    "@astrojs/starlight": "^0.40.0"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.9",
    "typescript": "^5.7.0"
  }
}
