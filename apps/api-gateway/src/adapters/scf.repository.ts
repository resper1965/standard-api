/**
 * SCF Drizzle Repository — re-exported from @standard/schemas (canonical source).
 *
 * This file previously contained a 1,461-line copy of the repository.
 * The canonical implementation lives in packages/schemas/src/db/scf.repository.ts.
 */
export { createDrizzleScfRepository } from "@standard/schemas";
