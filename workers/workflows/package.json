{
  "name": "@aegis/workflows",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "test": "tsx tests/run-tests.ts"
  },
  "dependencies": {
    "@aegis/assessment-engine": "workspace:*",
    "@aegis/schemas": "workspace:*"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "latest",
    "tsx": "^4.21.0",
    "typescript": "latest",
    "wrangler": "latest"
  }
}
